@model IEnumerable<Capstones.Models.Events>

@{
    ViewBag.Title = "index";
}

<h2>Events</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.DateTImeOfEvent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Attendees)
        <th>
            @Html.DisplayNameFor(model => model.KeyWord)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.DateTImeOfEvent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Attendees)
        </td>
        <td>@Html.DisplayFor(ModelItem => item.KeyWord)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
